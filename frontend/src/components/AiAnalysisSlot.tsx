import { Paper } from '@mantine/core';

export default function AiAnalysisSlot() {
  // Empty mount point for future AI review analysis. Keep this region free of UI output for now.
  return <Paper className="ai-analysis-slot" aria-hidden="true" data-ai-analysis-slot />;
}
